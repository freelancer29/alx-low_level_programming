#include <stdio.h>

/**
 * main - The entry point for all C program
 * @void: Doesn't take any parameters
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
int x = 1;
printf("%d\n", x);
return (0);
}
